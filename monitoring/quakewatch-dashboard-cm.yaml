apiVersion: v1
kind: ConfigMap
metadata:
  name: quakewatch-grafana-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"                 # <- Grafana sidecar watches this
    app.kubernetes.io/instance: monitoring
data:
  quakewatch-dashboard.json: |
