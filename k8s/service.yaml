apiVersion: v1
kind: Service
metadata:
  name: quakewatch
  namespace: quakewatch
spec:
  selector:
    app: quakewatch
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: ClusterIP
